/* C programs utilize functions.
 * Many functions are helpfully
 * predefined in headers. Specifically,
 * printf() is defined in stdio.h
 */
#include <stdio.h>

/* technically we should specify what `main` is */
/* main receives no arguments, hence ()         */
main() {
    /* main calls printf(), which prints
     * "hello world", and starts a new line
     */
    printf("hello, world\n");
}
